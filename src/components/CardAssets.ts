// Card assets utility
// This file can be used to reference card images if you add image assets later

export const getCardImagePath = (suit: string, rank: string): string => {
  // If you add card images, uncomment and use this:
  // return require(`../../assets/cards/${suit}_${rank}.png`);
  return '';
};

export const getCardBackPath = (): string => {
  // If you add a card back image, uncomment and use this:
  // return require('../../assets/cards/card_back.png');
  return '';
};

